/**
 * Write a program that prints all elements of the array multiplied by 2, without using loops.
 * Hint: Google js array map
 */

var array = [1, 2, 3];

const map1 = array.map(i => i * 2);

console.log(map1);
